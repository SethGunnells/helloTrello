import * as React from 'react';
import * as ReactDOM from 'react-dom';
import { HelloTrelloApp } from './HelloTrelloApp.jsx';

ReactDOM.render(
  React.createElement(HelloTrelloApp),
  document.getElementById('app-root')
);
